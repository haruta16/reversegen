[package]
name = "reversegen-strategy-sim"
version = "0.1.0"
edition = "2021"
description = "Rust policy simulation engine for ReverseGen batch strategies"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
